using UnityEngine;
using System.Collections;
using UnityEngine.UI;
public class BurntGameObject : MonoBehaviour {
    public float lifeTime;
	// Use this for initialization
	void Start () {
        gameObject.SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
